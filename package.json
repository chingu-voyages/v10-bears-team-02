{
    "name": "gardenguru-app",
    "version": "0.1.0",
    "engines": {
        "node": "11.10.0",
        "npm": "6.7.0"
      },
    "scripts": {       
        "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix server && npm install --prefix client"
    }
}